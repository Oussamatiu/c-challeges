#include <stdio.h>

void main(){
    // int a , b , c ;
    // float  x1 ,x2 ,delta ;
    // delta = pow(b , 2)-4 * a * c;
    // if (delta > 0)
    // {
    //     x1 = -b + sqrt(delta) / 2*a;
    //     x2 = -b - sqrt(delta) / 2*a;
    //     printf("deux solution : %.2f et ");
    // }
    if(6){
        printf("dd");
    }
    
}